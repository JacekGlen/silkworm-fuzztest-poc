

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAMES ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS} CMAKE_FIND_ROOT_PATH_BOTH)
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAMES ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                string(REGEX REPLACE "[^A-Za-z0-9.+_-]" "_" _LIBRARY_NAME ${_LIBRARY_NAME})
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated Findroaring.cmake")
# Global approach
set(roaring_FOUND 1)
set(roaring_VERSION "1.1.2")

find_package_handle_standard_args(roaring REQUIRED_VARS
                                  roaring_VERSION VERSION_VAR roaring_VERSION)
mark_as_advanced(roaring_FOUND roaring_VERSION)


set(roaring_INCLUDE_DIRS "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/include")
set(roaring_INCLUDE_DIR "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/include")
set(roaring_INCLUDES "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/include")
set(roaring_RES_DIRS )
set(roaring_DEFINITIONS )
set(roaring_LINKER_FLAGS_LIST
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,MODULE_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>"
)
set(roaring_COMPILE_DEFINITIONS )
set(roaring_COMPILE_OPTIONS_LIST "" "")
set(roaring_COMPILE_OPTIONS_C "")
set(roaring_COMPILE_OPTIONS_CXX "")
set(roaring_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(roaring_LIBRARIES "") # Will be filled later
set(roaring_LIBS "") # Same as roaring_LIBRARIES
set(roaring_SYSTEM_LIBS )
set(roaring_FRAMEWORK_DIRS )
set(roaring_FRAMEWORKS )
set(roaring_FRAMEWORKS_FOUND "") # Will be filled later
set(roaring_BUILD_MODULES_PATHS )

conan_find_apple_frameworks(roaring_FRAMEWORKS_FOUND "${roaring_FRAMEWORKS}" "${roaring_FRAMEWORK_DIRS}")

mark_as_advanced(roaring_INCLUDE_DIRS
                 roaring_INCLUDE_DIR
                 roaring_INCLUDES
                 roaring_DEFINITIONS
                 roaring_LINKER_FLAGS_LIST
                 roaring_COMPILE_DEFINITIONS
                 roaring_COMPILE_OPTIONS_LIST
                 roaring_LIBRARIES
                 roaring_LIBS
                 roaring_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to roaring_LIBS and roaring_LIBRARY_LIST
set(roaring_LIBRARY_LIST roaring)
set(roaring_LIB_DIRS "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/lib")

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_roaring_DEPENDENCIES "${roaring_FRAMEWORKS_FOUND} ${roaring_SYSTEM_LIBS} ")

conan_package_library_targets("${roaring_LIBRARY_LIST}"  # libraries
                              "${roaring_LIB_DIRS}"      # package_libdir
                              "${_roaring_DEPENDENCIES}"  # deps
                              roaring_LIBRARIES            # out_libraries
                              roaring_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "roaring")                                      # package_name

set(roaring_LIBS ${roaring_LIBRARIES})

foreach(_FRAMEWORK ${roaring_FRAMEWORKS_FOUND})
    list(APPEND roaring_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND roaring_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${roaring_SYSTEM_LIBS})
    list(APPEND roaring_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND roaring_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(roaring_LIBRARIES_TARGETS "${roaring_LIBRARIES_TARGETS};")
set(roaring_LIBRARIES "${roaring_LIBRARIES};")

set(CMAKE_MODULE_PATH "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/home/jacek/.conan/data/roaring/1.1.2/_/_/package/403715985e739021a7822a0eafab35792cdeb1ac/" ${CMAKE_PREFIX_PATH})

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET roaring::roaring)
        add_library(roaring::roaring INTERFACE IMPORTED)
        if(roaring_INCLUDE_DIRS)
            set_target_properties(roaring::roaring PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${roaring_INCLUDE_DIRS}")
        endif()
        set_property(TARGET roaring::roaring PROPERTY INTERFACE_LINK_LIBRARIES
                     "${roaring_LIBRARIES_TARGETS};${roaring_LINKER_FLAGS_LIST}")
        set_property(TARGET roaring::roaring PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${roaring_COMPILE_DEFINITIONS})
        set_property(TARGET roaring::roaring PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${roaring_COMPILE_OPTIONS_LIST}")
        
    endif()
endif()

foreach(_BUILD_MODULE_PATH ${roaring_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()
